# Single shared virtualenv for local dev. Each service still runs as its own
# process (api, mcp_server, twin_service, simulator) - this just avoids juggling
# five venvs on one dev machine.
-e ./libs/common

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
apscheduler>=3.10
google-cloud-firestore>=2.16
google-auth>=2.29
groq>=0.9
mcp>=1.2
langgraph>=0.2
langchain-core>=0.3
httpx>=0.27
numpy>=1.26
scikit-learn>=1.4
sentence-transformers>=3.0
websockets>=12.0
python-dotenv>=1.0
